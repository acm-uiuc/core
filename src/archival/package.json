{
  "name": "infra-core-archival",
  "version": "1.0.0",
  "description": "Archives DynamoDB TTL-deleted items to Firehose",
  "license": "BSD-3-Clause",
  "author": "ACM@UIUC",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc && node build.js",
    "format": "oxfmt --check \"*.ts\" \"**/*.ts\"",
    "lint": "oxlint .",
    "format:write": "oxfmt --write \"*.ts\" \"**/*.ts\""
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.1093.0",
    "@aws-sdk/client-firehose": "^3.1093.0",
    "@aws-sdk/util-dynamodb": "^3.996.7"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.161"
  }
}
